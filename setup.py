from setuptools import find_packages, setup

setup(
    name='awsclient',
    packages=find_packages(),
    version='0.1.3',
    description='Custom ampf botocore lib',
    author='FT',
    license='',
    install_requires=[],
    setup_requires=[],
)
